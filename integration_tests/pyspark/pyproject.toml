[project]
name = "pyspark-integration"
version = "0.1.0"
description = "An integration test for querying sal-built iceberg tables with PySpark"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.2.0",
    "pytest>=9.1.1",
    "shapely>=2.1.2",
]
