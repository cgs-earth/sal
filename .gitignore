testdata/latest
__pycache__
.tmp/
sal_cli
*.nq.gz
.sal
sal-cli
sal
.sal/data
.env
.venv/

# Demo GIFs are recorded by .github/workflows/demos.yml on every push rather than
# committed, so they can never show output the CLI no longer produces.
docs/demos/.bin/
docs/public/demos/