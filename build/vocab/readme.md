This directory contains any vocabularies that are used by SAL that can't be dereferenced or fetched online for some reason. In general the map is as follows:

- `$URL_ENCODED_VOCABULARY_URL.$RDF_FORMAT`

So for instance `https://schema.org/` maps to `https%3A%2F%2Fschema.org.jsonld`